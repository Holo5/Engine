export class PlayerRank {
    public rank: number;
    public isAmbassador: boolean;

    constructor(rank: number, isAmbassador: boolean) {
        this.rank = rank;
        this.isAmbassador = isAmbassador;
    }
}
